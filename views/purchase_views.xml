<odoo>
    <data>

        <record id="coinsamatik_purchase_order_view_form" model="ir.ui.view">
            <field name="name">coinsamatik_purchase_order_view_form</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="attributes">
                    <attribute name="domain">['|',('company_id','=',False),('company_id','=',company_id),('purchase_user_id','=', current_user_id)]</attribute>
                </field>
            </field>
        </record>
        
    </data>
</odoo>